<br/>
<div class="alert alert-info" role="alert">
	This is a very simple page: all it does is add a cookie which we should see redacted in the response data when sent to APM Server. <br/>
	If the APM .NET Agent is activated then the request is captured by the agent and you can see it in Kibana including the captured header.
</div>